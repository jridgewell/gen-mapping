const {
  GenMapping,
  addSegment,
  addMapping,
  setSourceContent,
  decodedMap,
  encodedMap,
  allMappings,
} = require('..');
const assert = require('assert');

describe('GenMapping', () => {});
